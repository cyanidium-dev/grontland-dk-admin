/**
 * Schema registry. Content model arrives per the outline
 * (grotland-workspace/docs/cms-then-i18n-outline.md, Phase 2):
 * shared locale objects (da/en) → siteSettings → galleryCategory /
 * project / service documents → page singletons → lead.
 */
export const schemaTypes = []
